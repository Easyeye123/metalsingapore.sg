# Sample environment for the Ezzogenics hub site.
# Copy to `.env` for local tooling — nothing in `.env` is currently required
# at build time. The portfolio admin keeps its own secrets in
# /portfolio-admin/config/credentials.json (mode 0600, .htaccess-protected).

# Example operational settings — purely informational right now.
SITE_ORIGIN=https://ezzogenics.com
CONTACT_TO_EMAIL=david@ezzogenics.com
